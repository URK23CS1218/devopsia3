apiVersion: v1
kind: Service
metadata:
  name: devsecops-webapp-service
  namespace: devsecops-app
  labels:
    app: devsecops-webapp
spec:
  type: ClusterIP
  selector:
    app: devsecops-webapp
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
